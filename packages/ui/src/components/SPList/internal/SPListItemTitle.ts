// Utilities
import { createSimpleFunctional } from '@/utils'

export const SPListItemTitle = createSimpleFunctional('sp-list-item-title')

export type SPListItemTitle = InstanceType<typeof SPListItemTitle>
